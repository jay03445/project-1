This is the JWSK server:
This is how you run it in PowerShell terminal:
 python -m pytest --cov=app --cov=tests
 This is how you run it in Gradebot second terminal:
 .\gradebot.exe project-1 --run="python server.py"
